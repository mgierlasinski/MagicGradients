<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:Playground.Views"
       Title="Playground"
       x:Class="Playground.AppShell">

    <Shell.Resources>
        <ResourceDictionary>
            <Color x:Key="NavigationPrimary">#2196F3</Color>
            <Style x:Key="BaseStyle" TargetType="Element">
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource NavigationPrimary}" />
                <Setter Property="Shell.ForegroundColor" Value="White" />
                <Setter Property="Shell.TitleColor" Value="White" />
                <Setter Property="Shell.DisabledColor" Value="#B4FFFFFF" />
                <Setter Property="Shell.UnselectedColor" Value="#95FFFFFF" />
                <Setter Property="Shell.TabBarBackgroundColor" Value="{StaticResource NavigationPrimary}" />
                <Setter Property="Shell.TabBarForegroundColor" Value="White"/>
                <Setter Property="Shell.TabBarUnselectedColor" Value="#95FFFFFF"/>
                <Setter Property="Shell.TabBarTitleColor" Value="White"/>
            </Style>
            <Style TargetType="ShellItem" BasedOn="{StaticResource BaseStyle}" />
        </ResourceDictionary>
    </Shell.Resources>

    <Shell.FlyoutHeaderTemplate>
        <DataTemplate>
            <Grid Padding="20,40">
                <Label Text="This is true magic!" FontSize="Medium" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" />
            </Grid>
        </DataTemplate>
    </Shell.FlyoutHeaderTemplate>

    <FlyoutItem Title="Linear Gradients">
        <Tab>
            <ShellContent Title="Linear Gradients" ContentTemplate="{DataTemplate local:LinearGradientsPage}" />
            <ShellContent Title="Repeating Gradients" ContentTemplate="{DataTemplate local:RepeatingLinearGradientsPage}" />
            <ShellContent Title="Linear Gradients in CSS" ContentTemplate="{DataTemplate local:LinearGradientsPageCss}" />
            <ShellContent Title="Repeating Gradients in CSS" ContentTemplate="{DataTemplate local:RepeatingLinearGradientsPageCss}" />
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="Radial Gradients">
        <Tab>
            <ShellContent Title="Radial Gradients" ContentTemplate="{DataTemplate local:RadialGradientsPage}" />
        </Tab>
    </FlyoutItem>

    <ShellContent Title="Paste CSS" ContentTemplate="{DataTemplate local:PasteCssPage}" />
    <ShellContent Title="Gallery" ContentTemplate="{DataTemplate local:GalleryListPage}" />

</Shell>
