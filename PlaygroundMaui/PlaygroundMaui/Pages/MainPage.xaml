<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:PlaygroundMaui.Pages;assembly=PlaygroundMaui"
             xmlns:gski="clr-namespace:MagicGradients.Graphics.Skia;assembly=MagicGradients.Graphics.Skia"
             x:Class="PlaygroundMaui.Pages.MainPage"
             NavigationPage.HasNavigationBar="False">
    <StackLayout>
        <Grid>
            <gski:GradientView StyleClass="RainbowGradient" />
            <Label HorizontalTextAlignment="Center" TextColor="White" Margin="24">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="Magic Gradients" FontSize="36" />
                        <Span Text="&#x0a;using Microsoft.Maui.Graphics" FontSize="18" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>
        </Grid>
        <StackLayout Margin="20">
            <Button Text="Linear Gradients" Command="{pages:Navigate {x:Type pages:LinearPage}}" />
            <Button Text="Radial Gradients" Command="{pages:Navigate {x:Type pages:RadialPage}}" />
            <Button Text="Masks" Command="{pages:Navigate {x:Type pages:MasksPage}}" />
            <Button Text="Gallery" Command="{pages:Navigate {x:Type pages:CategoriesPage}}" />
        </StackLayout>
    </StackLayout>
</ContentPage>
