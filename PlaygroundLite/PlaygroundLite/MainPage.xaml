<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:magic="clr-namespace:MagicGradients;assembly=MagicGradients"
             x:Class="PlaygroundLite.MainPage">

    <Grid>
        <magic:GradientView x:Name="GradientView" HorizontalOptions="Fill" VerticalOptions="Fill">
            <magic:LinearGradient>
                <magic:GradientStop Color="Red" />
                <magic:GradientStop Color="Green" />
                <magic:GradientStop Color="Blue" />
            </magic:LinearGradient>
        </magic:GradientView>
        <StackLayout VerticalOptions="End" HorizontalOptions="Center" Orientation="Horizontal">
            <Button Text="Rotate [-]" Clicked="RotateLeft_Clicked" />
            <Button Text="Add" Clicked="AddStop_Clicked" />
            <Button Text="Remove" Clicked="RemoveStop_Clicked" />
            <Button Text="Rotate [+]" Clicked="RotateRight_Clicked" />
        </StackLayout>
    </Grid>

</ContentPage>
