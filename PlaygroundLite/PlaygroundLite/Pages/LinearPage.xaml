<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:magic="clr-namespace:MagicGradients;assembly=MagicGradients"
             x:Class="PlaygroundLite.Pages.LinearPage"
             Title="Linear gradient">
    <StackLayout>
        <magic:GradientView x:Name="GradientView" GradientSource="{Binding Gradient}" HorizontalOptions="Fill" VerticalOptions="FillAndExpand" />
        <StackLayout Margin="10">
            <StackLayout Orientation="Horizontal">
                <Label Text="Color stops" FontSize="Medium" />
                <Label Text="{Binding StopsCount}" FontSize="Medium" HorizontalOptions="EndAndExpand" />
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Button Text="Add" Command="{Binding AddStopCommand}" HorizontalOptions="FillAndExpand" />
                <Button Text="Remove" Command="{Binding RemoveStopCommand}" HorizontalOptions="FillAndExpand" />
            </StackLayout>
            <Label Text="Angle" FontSize="Medium" Margin="0,10,0,0" />
            <StackLayout Orientation="Horizontal">
                <Stepper Value="{Binding Angle}" Minimum="-360" Maximum="360" Increment="10" />
                <Entry Text="{Binding Angle}" HorizontalTextAlignment="End" HorizontalOptions="FillAndExpand" />
            </StackLayout>
        </StackLayout>
    </StackLayout>
</ContentPage>